mllint https://github.com/bvobart/mllint

We use mllint directly.

environment:

		Python 3.8
		ubuntu 20.04
 
usage:

		pip install mllint
		$ mllint ${dir} > ${dir}.md
		
dir is your dictionaty to detect
